import { LoginForm } from "@/features/auth/components/LoginForm";

export const metadata = { title: "Iniciar sesión | Hoja de Parra Spitz" };

export default function LoginPage() {
  return <LoginForm />;
}