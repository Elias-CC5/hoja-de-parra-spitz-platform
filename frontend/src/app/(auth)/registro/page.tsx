import { RegisterForm } from "@/features/auth/components/RegisterForm";

export const metadata = { title: "Crear Cuenta | Hoja de Parra Spitz" };

export default function RegisterPage() {
  return <RegisterForm />;
}